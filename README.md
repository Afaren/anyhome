# anyhome
9/8/2015 
				1. 房屋管理功能中，直接在jsp页面上嵌入了java代码，而且重复利用了一段代码。明白这里应该抽象出一个公用的函数，
			        但是时间紧张，暂且这样做。后面再做修改。
			    2. mainScreen需要管理，一个未登录的，一个登陆后的，其它的全部应该丢弃了
			    3. manager考虑不妨在web-inf下了。加个过滤器过滤 /manager/*(使用servlet进入)，内部的跳转全部使用servlet的转发吧(初步考虑）
			    4. 页面之间的跳转暂且不管了，将所有页面的功能实现了再做考量。
			        
			     
